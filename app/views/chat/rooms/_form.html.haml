%li.chat-room{ 'data-chat-room-id' => @room.id }
  .chat-room-wrapper
    .chat-room-handler
      = link_to @room.name, "#", :class => 'chat-room-trigger'
    .chat-room-container
      .chat-messages-wrapper
      = form_tag chat_room_messages_path(@room), :remote => true, :class => "chat-room-form" do
        = text_area_tag :message