.row
  .span7.comments
    - if can? :create, @comment
      = form_for [@article,@comment], :html=>{:class => 'form-stacked'} do |f|
        %fieldset
          .control-group
            = f.text_area :content, placeholder: 'Write a comment...'
          .control-group.submit
            .controls
              %span.help-inline
                %span.character-counter 0
                = "/ 300 characters used"
              = f.submit "Comment", :class => 'btn'
    - else
      %h6.help-inline Comments disabeled...