.row
  .span16
    %ul.faqs.unstyled{ class: can?(:update, Faq) ? 'enable_sort' : ''}
      - @faqs.each do |faq|
        = render :partial => 'faq', locals: { faq: faq }

- if can? :create, @faq
  .row
    .span16
      = form_for @faq, remote: true, validate: true, html: { class: 'form-stacked' } do |f|
        %fieldset
          .control-group
            = f.label :question, "Question"
            .controls
              = f.text_field :question, class: 'span12'
          .control-group
            = f.label :answer, "Answer"
            .controls
              = f.text_area :answer, class: 'span12'
          .form-actions
            = f.submit "Save", class: 'btn btn-primary'