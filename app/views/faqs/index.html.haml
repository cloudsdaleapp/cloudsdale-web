.page-header
  %h1 FAQ
  %p
    = "If you got any other questions, want to request features that you'd see fit on Cloudsdale or just show some appreciation. Send an email to: "
    = mail_to("zeeraw@cloudsdale.org")
    = " where I will try to answer your questions individually."

.row
  .span16
    %ul.faqs.unstyled{ class: can?(:update, Faq) ? 'enable_sort' : ''}
      - @faqs.each do |faq|
        = render :partial => 'faq', locals: { faq: faq }

- if can? :create, @faq
  .row
    .span16
      = form_for @faq, remote: true, validate: true, html: { class: 'form-stacked' } do |f|
        %fieldset
          .clearfix
            = f.label :question, "Question"
            .input
              = f.text_field :question, class: 'xxlarge'
          .clearfix
            = f.label :answer, "Answer"
            .input
              = f.text_area :answer, class: 'xxlarge'
          .actions
            = f.submit "Save", :disable_with => 'Saving...', class: 'btn primary'
