.page-header
  %h2= "Meet the Crew"
  %p.lead= "We're the ones behind Cloudsdale who make your stay worthwhile."

- @users = User.where(:role.in => [6,5,4,3]).order_by(role: :desc)
%ul.thumbnails
  - @users.each do |user|
    %li.clearfix.span3
      .thumbnail
        = image_tag user.avatar.url, class: 'img-polaroid', style: 'max-width: 100px; height: 100px; margin: 25px auto 10px auto;'
        .caption
          %center
            %h4{ style: 'height: 40px;'}= user.name
            %span.badge= user.symbolic_role
