= image_tag "logo/cloudsdale-logo-medium.png"

= form_for @user, :url => update_password_user_path(@user), method: :put, class: 'form-stacked' do |f|
  %center
    = render :partial => 'layouts/alert'
  %fieldset
    = f.password_field :password, :placeholder => "New Password"
    = f.password_field :password_confirmation, :placeholder => "Confirm..."
    = submit_tag "Change\t»", :class => 'btn success'
    
%nav.options
  = link_to "Saddle up", register_path
  = link_to "Log out", logout_path
  = link_to "Help", "help"
