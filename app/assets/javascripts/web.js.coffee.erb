#= require jquery
#= require foundation
#= require handlebars
#= require ember
#= require ember-data
#= require_self
#
#= require ./web/cloudsdale
window.Cloudsdale = Ember.Application.create

  rootElement: 'body'

  LOG_TRANSITIONS: <%= true || !Rails.env.production? %>
  LOG_TRANSITIONS_INTERNAL: <%= true || !Rails.env.production? %>

  ready: -> $(document).foundation()

