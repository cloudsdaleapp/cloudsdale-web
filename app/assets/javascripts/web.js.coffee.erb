#= require jquery-2.0.3
#= require foundation
#= require handlebars
#= require ember
#= require ember-data
#= require_self
#
#= require ./web/cloudsdale

window.Cloudsdale = Ember.Application.create

  rootElement: 'body'

  LOG_TRANSITIONS: true #<%= true || !Rails.env.production? %>
  LOG_TRANSITIONS_INTERNAL: true #<%= true || !Rails.env.production? %>

  ready: -> $(document).foundation()
