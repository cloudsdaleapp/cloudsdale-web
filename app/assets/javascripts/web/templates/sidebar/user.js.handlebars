 <div>
  {{#linkTo 'root' class=left}}
    <span class="avatar small primary" />
      <img {{bindAttr src="currentUser.avatar" }}/>
    </span>
  {{/linkTo}}
  <hgroup>
    <h4>{{#linkTo 'root' class=left}}{{currentUser.displayName}}{{/linkTo}}</h4>
  </hgroup>
</div>