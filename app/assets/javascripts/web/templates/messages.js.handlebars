<div class="messages">
  <br />
  {{#each this}}
    {{render 'message' this.content}}
  {{/each}}
</div>

{{#with currentMessage}}
  <form class="new-message" {{action 'saveMessage' on='submit'}}>
    <div class="row">
      <div class="12-large 12-small columns">
        {{input value=content type='text'}}
      </div>
    </div>
  </form>
{{/with}}