<div class="row">
  <div class="small-12 large-12 columns">

    {{#each messages}}
      <div class="left avatar small content">
        <img {{bindAttr src="this.author.avatar"}} />
      </div>
      <h6>{{this.author.displayName}} <small>@{{this.author.username}}</small></h6>
      <p>{{this.content}}</p>
    {{/each}}

  </div>
</div>